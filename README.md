This is a simple demo for React and Vite.

This application allows users to manage employee records with features like creating, viewing, sorting, and refreshing employee data.

Hot to run:

1. clone the repo
2. Install dependencies - mpn install
3. npm start
4. FrontEnd will run on port 3030
